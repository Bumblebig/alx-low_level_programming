#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 * _strlen - returns the length of string
 * @s: Int value for the argument of the function
 * Return: 0
 */

int _strlen(char *s)
{
	return (strlen(s));
}
