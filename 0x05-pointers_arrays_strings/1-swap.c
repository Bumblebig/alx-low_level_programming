#include "main.h"
#include <stdio.h>

/**
 * swap_int - Code to swap the values of int a and b
 * @a: First Int value
 * @b: Second int value
 * Return: 0
 */

void swap_int(int *a, int *b)
{

int ex;

ex = *a;
*a = *b;
*b = temp;
}
