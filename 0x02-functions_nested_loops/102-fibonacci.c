#include <stdio.h>

/**
 * main - Entry point
 * Return: 0
 */

int main(void)

{

	int tot;
	unsigned long a = 0, b = 1, ans;

	for (tot = 0; tot < 50; tot++)
	{

		sum = fib1 + fib2;
		printf("%lu", sum);
		
		fib1 = fib2;
		fib2 = sum;

		if (count == 49)
			printf("\n");

		else
			printf(", ");
		
	}
	return (0);
}
